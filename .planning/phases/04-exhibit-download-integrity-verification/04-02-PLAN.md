---
phase: 04-exhibit-download-integrity-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/client.ts
  - tests/client.test.ts
autonomous: true

must_haves:
  truths:
    - User can call EdgarClient.downloadExhibit() to download exhibit bytes
    - Downloaded exhibit includes cryptographic integrity verification (SHA-256)
    - Client integration tests verify full download flow works end-to-end
  artifacts:
    - path: "src/client.ts"
      provides: "EdgarClient.downloadExhibit() integration with DownloadService"
      contains: "private readonly downloadService"
      min_lines: 70
    - path: "tests/client.test.ts"
      provides: "EdgarClient download integration tests"
      contains: "downloadExhibit"
      min_lines: 250
  key_links:
    - from: "src/client.ts"
      to: "src/download/service.ts"
      via: "DownloadService import and initialization"
      pattern: "import.*DownloadService.*from.*download"
    - from: "src/client.ts"
      to: "this.downloadService.downloadExhibit"
      via: "method delegation"
      pattern: "this\\.downloadService\\.downloadExhibit"
---

<objective>
Wire DownloadService to EdgarClient, completing the public API for exhibit download with integrity verification.

Purpose: Expose download functionality to library consumers; validates Phase 4 deliverables work end-to-end.

Output: EdgarClient.downloadExhibit() fully functional, replacing "Not yet implemented" stub, with integration tests confirming complete download flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exhibit-download-integrity-verification/04-RESEARCH.md
@.planning/phases/04-exhibit-download-integrity-verification/04-01-SUMMARY.md
@src/client.ts
@src/download/service.ts
@tests/client.test.ts
</context>

<tasks>

<task type="auto">
  <name>Wire DownloadService to EdgarClient and implement downloadExhibit method</name>
  <files>
src/client.ts
  </files>
  <action>
Modify `src/client.ts` to integrate DownloadService:

1. Import DownloadService:
   - Add to imports: `import { DownloadService } from "@/download"`

2. Add private field to EdgarClient class:
   - After `private readonly exhibitService: ExhibitService` add:
   - `private readonly downloadService: DownloadService`

3. Initialize DownloadService in constructor:
   - After `this.exhibitService = new ExhibitService(this.httpClient)` add:
   - `this.downloadService = new DownloadService(this.httpClient)`

4. Implement downloadExhibit method (replace "Not yet implemented" stub):
   - Replace existing stub body with:
   - `return this.downloadService.downloadExhibit(exhibit)`
   - Keep method signature: `async downloadExhibit(exhibit: ExhibitRef): Promise<DownloadedExhibit>`

Follow Phase 3 Plan 03 wiring pattern (mirrors ExhibitService integration).
  </action>
  <verify>
Typecheck passes: `pnpm typecheck`
Build succeeds: `pnpm build`
  </verify>
  <done>
EdgarClient.downloadExhibit() delegates to DownloadService, "Not yet implemented" stub removed, DownloadService initialized in constructor.
  </done>
</task>

<task type="auto">
  <name>Add EdgarClient integration tests for downloadExhibit</name>
  <files>
tests/client.test.ts
  </files>
  <action>
Add integration tests to `tests/client.test.ts` for downloadExhibit() method, following existing test patterns (mocked global fetch):

Tests to add (minimum 6):

1. **downloadExhibit returns complete DownloadedExhibit**
   - Mock fetch to return binary response with Content-Type header
   - Call client.downloadExhibit(exhibitRef)
   - Verify result has exhibit, bytes, sizeBytes, mimeType, sha256 fields
   - Verify bytes is Uint8Array
   - Verify sha256 is 64-character lowercase hex string

2. **downloadExhibit extracts MIME type from Content-Type**
   - Mock fetch with Content-Type: "text/html; charset=utf-8"
   - Verify result.mimeType === "text/html" (charset stripped)

3. **downloadExhibit handles missing Content-Type**
   - Mock fetch without Content-Type header
   - Verify result.mimeType === undefined

4. **downloadExhibit computes correct SHA-256 digest**
   - Mock fetch with known binary data
   - Pre-compute expected SHA-256 for mock data
   - Verify result.sha256 matches expected digest

5. **downloadExhibit returns correct sizeBytes**
   - Mock fetch with specific byte count
   - Verify result.sizeBytes matches mock data length

6. **downloadExhibit uses exhibit.exhibitUrl**
   - Mock fetch and capture called URL
   - Verify fetch called with exhibit.exhibitUrl

Mock pattern (mirrors existing client tests):
```typescript
globalThis.fetch = vi.fn(async (url) => {
  return {
    ok: true,
    status: 200,
    headers: { get: (name) => name === "Content-Type" ? "text/html" : null },
    arrayBuffer: async () => new Uint8Array([/* mock data */]).buffer,
  }
})
```

Use existing helper functions (createMockFilingRef, createMockExhibitRef) if available, or create new helpers for exhibit refs.

Group tests in `describe("downloadExhibit", () => { ... })` block.
  </action>
  <verify>
Run client tests: `pnpm vitest run tests/client.test.ts`
At least 6 new downloadExhibit tests pass.
Run full test suite: `pnpm test:run`
All tests pass (no regressions).
  </verify>
  <done>
At least 6 EdgarClient.downloadExhibit() integration tests added and passing, all tests verify complete download flow with metadata extraction and SHA-256 computation.
  </done>
</task>

</tasks>

<verification>
1. Run client tests: `pnpm vitest run tests/client.test.ts`
2. Run full test suite: `pnpm test:run`
3. Typecheck: `pnpm typecheck`
4. Lint: `pnpm lint`
5. Build: `pnpm build`
6. Verify no "Not yet implemented" errors in client.ts
</verification>

<success_criteria>
- ✓ DownloadService imported and initialized in EdgarClient constructor
- ✓ EdgarClient.downloadExhibit() delegates to downloadService.downloadExhibit()
- ✓ "Not yet implemented" stub removed from downloadExhibit method
- ✓ At least 6 client integration tests added for downloadExhibit
- ✓ Tests verify DownloadedExhibit fields (exhibit, bytes, sizeBytes, mimeType, sha256)
- ✓ Tests verify MIME type extraction with and without charset
- ✓ Tests verify SHA-256 digest correctness
- ✓ Tests verify sizeBytes accuracy
- ✓ All prior tests still pass (no regressions)
- ✓ Typecheck, lint, and build clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-exhibit-download-integrity-verification/04-02-SUMMARY.md`
</output>
